<% include ../partials/header %>
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-8 offset-lg-2 col-md-6 offset-md-3 col-sm-12">
                <h1 class="text-center"><%= item.name %></h1>
                <div class="img-lg">
                    <img class="img-fluid rounded" src="<%= item.image %>" alt="Card image cap">
                    <div class="caption bg-dark white img-thumbnail">
                         <h4 class="float-right">Price: $<%= item.price %></h4>
                         <h4 class=""><%= item.price %></h4>
                         <a class="btn btn-primary btn-sm float-right" href="/products/item/<%=item._id%>/contact">Contact Seller</a>
                         <p><%= item.description %></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
<% include ../partials/footer %>